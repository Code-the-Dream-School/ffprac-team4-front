import styles from "./Messages.module.css";

const Messages = () => {
  return (
    <div>
      <h1>Messages</h1>
      <p>
        This is the page where users can view and send messages to other users.
      </p>
    </div>
  );
};

export default Messages;
